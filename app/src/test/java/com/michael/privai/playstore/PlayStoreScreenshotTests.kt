package com.michael.privai.playstore

/**
 * JUnit category for Play Store Roborazzi tests.
 * Included only when `-Pscreenshot` is set (see app/build.gradle.kts).
 */
interface PlayStoreScreenshotTests
